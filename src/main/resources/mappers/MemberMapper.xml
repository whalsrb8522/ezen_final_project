<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.myweb.www.repository.MemberDAO">
  <select id="getMember" resultType="com.myweb.www.domain.MemberVO">
  	select * from member where m_mail=#{m_mail}
  </select>
  
  <insert id="insertMember">
  	insert into member(m_mail, m_pw, m_nick_name, m_introduct, m_address)
  	values(#{m_mail}, #{m_pw}, #{m_nick_name}, #{m_introduct}, #{m_address})
  </insert>

<!--   <select id="idCheck" parameterType="String" resultType="int">
	select count(m_nick_name) from member WHERE m_nick_name = #{nickName}
  </select> -->
  
  
  
  </mapper>
  